name: Sync SmartThings history

on:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate SmartThings configuration
        env:
          SMARTTHINGS_TOKEN: ${{ secrets.SMARTTHINGS_TOKEN }}
          SMARTTHINGS_LOCATION_ID: ${{ secrets.SMARTTHINGS_LOCATION_ID }}
        run: |
          test -n "$SMARTTHINGS_TOKEN" || { echo "SMARTTHINGS_TOKEN secret is missing"; exit 1; }
          test -n "$SMARTTHINGS_LOCATION_ID" || { echo "SMARTTHINGS_LOCATION_ID secret is missing"; exit 1; }
          test "$SMARTTHINGS_LOCATION_ID" != "YOUR_LOCATION_ID" || { echo "SMARTTHINGS_LOCATION_ID still contains YOUR_LOCATION_ID"; exit 1; }

      - name: Fetch SmartThings history
        run: npm run sync-history
        env:
          SMARTTHINGS_TOKEN: ${{ secrets.SMARTTHINGS_TOKEN }}
          SMARTTHINGS_LOCATION_ID: ${{ secrets.SMARTTHINGS_LOCATION_ID }}
          SMARTTHINGS_HISTORY_URL: ${{ secrets.SMARTTHINGS_HISTORY_URL }}

      - name: Commit updated history
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add public/location-history.json
          git diff --cached --quiet || git commit -m 'Update location history'
          git push
